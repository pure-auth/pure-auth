# @pure-auth/core

Framework-agnostic TypeScript auth core for Pure Auth.

## Repository

This package is intended to live in its own repository:

- GitHub: `pure-auth/core`
- npm: `@pure-auth/core`

## Development

```bash
bun install
bunx tsc --noEmit
```
